% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_data.R
\name{climate_data}
\alias{climate_data}
\title{Retrieve climatological data}
\usage{
climate_data(
  region = NULL,
  state = NULL,
  city = NULL,
  max_cities = 5,
  delay = 1,
  add_site = TRUE,
  add_avg = TRUE
)
}
\arguments{
\item{region}{Character. Optional region name (e.g., "north", "northeast").}

\item{state}{Character. State abbreviation or full name (e.g., "se", "sergipe").}

\item{city}{Character. City name (partial or full; accent-insensitive).}

\item{max_cities}{Numeric. Maximum number of locations allowed to prevent excessive requests. Default is 5.}

\item{delay}{Numeric. Time delay in seconds between successive web requests. Default is 1.}

\item{add_site}{Logical. If TRUE, adds a site identifier column to the output.}

\item{add_avg}{Logical. If TRUE, computes monthly averages across locations (only when more than one location is selected).}
}
\value{
A tibble containing monthly climatological variables, including minimum temperature (tmin), maximum temperature (tmax), and precipitation (precip).
}
\description{
Retrieves monthly climatological data from Climatempo using flexible location inputs and automated web scraping.
}
\details{
Main function to retrieve climate data
}
\examples{
climate_data(state = "se", city = "aracaju")
}
